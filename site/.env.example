# No environment variables needed for Font Awesome.
# The FA Pro kit code (5cd93bd475) is hardcoded in src/layouts/Layout.astro.
# Kit codes are not secrets — they authenticate via allowed domains in the FA dashboard.
#
# If you ever rotate the kit or use a different kit per environment, add:
# PUBLIC_FA_KIT_CODE=your_kit_code_here
